{
  "version": 2,
  "buildCommand": "cd frontend && corepack yarn install --frozen-lockfile && corepack yarn build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
